<?xml version="1.0" encoding="utf-8"?>
<!--
  Adaptive icon (Android 8+). Single foreground + monochrome
  vector; the squircle background is supplied by the brand
  color so the launcher tile is the same dark green as the
  iOS AppIcon in both light and dark mode. The foreground is
  wrapped in an inset drawable so the leaf sits inside the
  launcher's safe zone instead of running off the edge.
-->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/palette_primary_light" />
    <foreground android:drawable="@drawable/ic_launcher_foreground_inset" />
    <monochrome android:drawable="@drawable/ic_launcher_foreground_inset" />
</adaptive-icon>
