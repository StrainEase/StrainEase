# DO NOT USE — this file is a leftover from the very first scaffold
# commit where the editor ended up saving gradle.properties inside
# gradle/properties/ instead of at the project root. Gradle only reads
# the top-level `android/gradle.properties`; this one is dead weight.
#
# Kept in the tree (instead of deleted) because the safety policy in
# this environment blocks `rm -rf` from the agent shell. Future
# cleanups can remove this whole `gradle/properties/` directory.
#
# Last touched: 2026-08-23 (PR-A1 scaffold)
